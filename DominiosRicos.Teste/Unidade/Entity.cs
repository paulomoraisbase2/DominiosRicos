using Xunit;

namespace DominiosRicos.Teste.Unidade
{
    public class Entity
    {
        [Fact]
        public void ClinicaValida()
        {
            //var clinica = new Clinica(Guid.NewGuid(), "Teste",
            //    "dev",
            //    new Periodo(DateTime.Now, default),
            //    new Email("phrmorais@hotmail.com"), new Endereco(default,
            //                                                    default,
            //                                                    default,
            //                                                    default,
            //                                                    default,
            //                                                    default,
            //                                                    default,
            //                                                    default));
            //clinica.AddTelefone(new Telefone("", "37 98833-6523", "Residencial"));
            ////Data data = new Data(new DateTime(2009, 6, 13));
            ////Console.WriteLine(data.Notifications);
            //Assert.True(clinica.Valid);
            //Mock<IClinicaRepositorio> mock = new Mock<IClinicaRepositorio>();
            //mock.Object.Add(clinica);

            //clinica.SetId(new long());
            //mock.Object.Add(clinica);

            //clinica.SetId(new long());
            //mock.Object.Add(clinica);

            //clinica.SetId(new long());
            //mock.Object.Add(clinica);

            //mock.Object.Commit();

            //var list = mock.Object.GetAll().ToList();
            ////VerificadorPrecoProduto verif = new VerificadorPrecoProduto();

            //// act
            //var resultadoEsperado = list.Count();
            //Assert.Equal(4, resultadoEsperado);
        }
    }
}